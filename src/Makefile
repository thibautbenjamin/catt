SOURCES = stdlib.ml settings.ml common.ml extSyntax.ml pShape.ml kernel.ml lexer.mll parser.mly command.ml prover.ml catt.ml
RESULT = catt
ANNOTATE = true

all: dnc
	./catt test.catt

include OCamlMakefile
